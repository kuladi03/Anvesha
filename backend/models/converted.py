import os
print(os.getcwd())  # Print the current working directory
print(os.path.exists('backend/models/dropout_risk_model.pkl'))  # Check if the file exists
print(os.path.exists('backend/models/label_encoders.pkl'))  # Check if the file exists
